# Only NEXT_PUBLIC_* vars belong here — these are the ones Next.js bundles
# into the browser and are the only frontend-specific values.
# All other env vars live in the project root .env (injected into the
# container by docker-compose).

NEXT_PUBLIC_API_URL=http://localhost:8000
